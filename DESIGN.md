# 24 点计算益智工具 · 网页版设计方案

> 版本：V1.0（设计稿）
> 定位：**纯前端网页应用**，打开即用、无需安装、支持手机与桌面双端
> 本文只做**方案设计与功能说明**，不含任何代码实现

---

## 一、产品定位与目标

### 1.1 一句话定位
一个以「24 点」为核心的**心算训练 + 竞技娱乐**平台：用多种玩法模式把同一套算术内核玩出不同体验，覆盖「休闲杀时间」「系统训练」「朋友对战」三类需求。

### 1.2 目标用户

| 用户类型 | 核心诉求 | 对应主打模式 |
|---|---|---|
| 小学高年级 ~ 初中生 | 提升四则运算熟练度、括号与运算顺序理解 | 教学引导模式、闯关模式 |
| 家长 | 陪练、可查看孩子的进步曲线 | 闯关 + 学习报告 |
| 成年人 / 上班族 | 碎片时间动脑、解压 | 每日挑战、无尽模式 |
| 朋友 / 同事 / 班级 | 实时对抗、比谁快 | 多人竞技（房间对战） |
| 硬核玩家 | 追求高难度、自定义规则 | 多张牌挑战、自定义目标数字 |

### 1.3 设计原则
1. **上手 3 秒**：首屏直接是可玩题目，不强制注册。
2. **一核多玩**：所有模式共用同一套「发牌—求解—校验—计分」内核。
3. **可难度量化**：难度不是拍脑袋，由求解器算出来。
4. **不惩罚探索**：错题、撤销、试算都不扣分，只有「提示」和「超时」影响得分。
5. **离线可用**：PWA，断网也能玩单机模式。

---

## 二、核心内核（所有模式共用）

### 2.1 求解器（Solver）
- 算法：**递归 + 分数精确运算**（用 `{分子, 分母}` 表示有理数，避免浮点误差，例如 `(1/3)` 不能写成 0.333）。
- 输入：N 张牌（N ≥ 2）+ 目标值 + 允许的运算符集合。
- 输出：
  - 是否可解；
  - 全部解（去重后的**本质不同解**，如 `a+b+c+d` 的交换顺序视为同一解）；
  - 最短/最"简单"的一条解（用于提示与教学）。
- 性能：4 张牌穷举规模约 `C(4,2) × 6 × 4 × 6 × 4 × 6 ≈ 数万` 次运算，毫秒级；5~7 张牌用 **Web Worker 后台线程**预计算，避免阻塞输入。

### 2.2 发牌器（Dealer）
- 支持「必可解发牌」与「随机发牌（可能无解）」两种策略：
  - 常规模式默认**必可解**，保证体验不卡死；
  - 「无解判定」玩法故意混入无解牌组，考察判断力。
- 支持「按难度发牌」：先随机生成候选牌组 → 求解器评估难度分 → 命中目标难度区间才发牌（带重试上限与兜底）。

### 2.3 表达式校验器
- 玩家提交表达式后，内核核验三件事：
  1. 用掉的牌与发牌**完全一致**（数量、数值一一对应）；
  2. 运算过程**合法**（无除以 0、中间结果均为有穷有理数）；
  3. 最终结果**等于目标值**。
- 校验失败时给出**精确错误定位**（如"你用了两张 7，但牌堆里只有一张 7"）。

### 2.4 输入方式（三选一，可并存）
| 方式 | 说明 | 适用端 |
|---|---|---|
| **点选式（主推）** | 点牌 A → 点运算符 → 点牌 B，生成新卡片；循环至剩一张 | 手机 / 触屏，防打字错误 |
| **键盘式** | 直接输入 `(3+3)*(8-4)`，实时语法高亮与括号配对 | 桌面 / 硬核玩家 |
| **拖拽式（进阶）** | 拖动牌到运算符两侧，形成表达式树 | 平板 / 教学演示 |

---

## 三、玩法模式规划（重点）

### 3.1 模式总览

| 编号 | 模式 | 牌数 | 目标值 | 时间 | 核心差异 | 优先级 |
|---|---|---|---|---|---|---|
| M1 | 经典 4 张牌 | 4 | 24 | 无/可选 | 基准体验 | P0 |
| M2 | 多张牌挑战 | 5 / 6 / 7 | 24 | 有 | 牌多、解法多，需"选子集"思维 | P0 |
| M3 | 限时闯关 | 4（递增） | 24 | 有 | 关卡制 + 生命值 + 星级 | P0 |
| M4 | 多人竞技 | 4 | 24 | 有 | 抢答、实时排名 | P1 |
| M5 | 自定义目标数字 | 2~7 | 用户定 | 可选 | 规则开放 | P1 |
| M6 | 每日挑战 | 4 | 24 | 有 | 全网同一题、排行榜 | P1 |
| M7 | 无尽 / 生存 | 4（递增） | 24 | 有 | 连续答题直到失败 | P2 |
| M8 | 教学 / 残局 | 4 | 24 | 无 | 分步引导 + 解法树可视化 | P2 |

---

### 3.2 M1 · 经典 4 张牌

**玩法**：发 4 张牌（A=1，J/Q/K=11/12/13，或可切换为 10），用 `+ − × ÷` 与括号凑出 24，每张牌用且仅用一次。

**子选项**
- 大牌开关：`J/Q/K = 11/12/13`（标准）或统一按 `10`（简化，适合低龄）。
- 是否允许「无解牌组」：关闭时保证每局有解。
- 计时：可关（练习）/ 可开（竞速）。

**特色交互**
- 点选式计算：已合成的中间结果会变成一张"新卡"留在桌面，玩家能直观看到表达式树的生长过程。
- 一键「撤销」「重置本局」。
- 求解完成后展示**解法数量**，并提供「看另一种解法」。

---

### 3.3 M2 · 多张牌挑战

**玩法**：发 5 / 6 / 7 张牌，规则分两种变体：

| 变体 | 规则 | 难度 | 说明 |
|---|---|---|---|
| **全用型** | 所有牌必须全部用完 | 高 | 组合爆炸，几乎必然有解，但极难找 |
| **选 4 型** | 从 N 张中任选 4 张凑 24 | 中 | 考验"先挑出可行子集"的判断力，存在多个解 |

**差异化设计**
- 提供「子集提示」：高亮一组**可解的 4 张牌**（消耗一次提示）。
- 计分随牌数放大（见第六节）。
- 7 张牌建议限时 180 秒起，且默认开启"草稿纸"（可手写标注中间值）。

---

### 3.4 M3 · 限时闯关

**结构**
- 章节 → 关卡：每章 12 关，共 8 章（96 关），难度随章节递增。
- 关卡类型混合：普通关 / BOSS 关（第 6、12 关，多张牌或极小解数）/ 奖励关（全对可跳关）。

**规则**
- 生命值：初始 3 颗心；答错或超时 −1；归零则关卡失败，可「复活」（看广告或消耗复活币，或等待恢复）。
- 单关限时：由难度等级决定（L1=90s，L5=45s）。
- 星级评定（决定章节奖励）：
  - ★ 通关
  - ★★ 通关 + 用时 < 50% 限时
  - ★★★ 通关 + 零提示 + 用时 < 25% 限时
- 章节解锁：前一章累计星星 ≥ 12 颗（可配置），避免卡关劝退。

---

### 3.5 M4 · 多人竞技

**两种形态**

| 形态 | 实现 | 场景 |
|---|---|---|
| **本地热座** | 同一设备轮流答题，各自计时 | 家庭、课堂，无需服务器 |
| **在线房间** | 房主生成 6 位房间码，2~8 人加入；服务端（或 P2P）同步题目与提交 | 远程朋友、班级群 |

**在线房间流程**
1. 房主创建房间 → 设置题数（5/10/20）、难度、是否允许提示 → 生成房间码 / 分享链接。
2. 玩家进入 → 准备 → 房主开始。
3. 所有人**同时看到同一副牌**，抢答：
   - 先提交且正确者得分（见 6.4）；
   - 提交错误者进入 **3 秒冻结**，期间不能提交；
   - 全部答对或时间耗尽 → 进入下一题（10 秒结算 + 展示最快解法）。
4. 全部题结束 → 结算页：得分排名、最快解题、正确率、MVP。

**观战与重放**：房间支持旁观席位，赛后可回放任意一题的「第一名解法」。

---

### 3.6 M5 · 自定义目标数字

**可自定义维度**
| 维度 | 取值范围 | 默认 |
|---|---|---|
| 目标值 | 1 ~ 100 的整数，或 0.5 / 1.5 等小数（进阶） | 24 |
| 牌数 | 2 ~ 7 张 | 4 |
| 牌面范围 | 1~10 / 1~13 / 自定义牌堆 | 1~13 |
| 运算符 | `+ − × ÷`，可选扩展：`平方`（单目）、`开方`、`取模`、`阶乘`、`拼接`（1和2拼成12） | `+ − × ÷` |
| 是否必须全用 | 是 / 否 | 是 |
| 是否允许无解 | 是 / 否 | 否（自动剔除无解牌组） |

**预设模板**：`凑 1`、`凑 100`、`只用加减`、`含阶乘的疯狂模式`、`小学作业模式（1~10、目标 20 以内）`。

**保存与分享**：自定义规则生成一条**分享码 / 链接**，别人打开即加载同一套规则，可投稿到「创意工坊」。

---

### 3.7 M6 · 每日挑战
- 每天 0 点刷新 **5 道题**（难度 L1→L5 递增），全网统一。
- 每日仅一次计分机会（可无限次练习但不计分）。
- 榜单：日榜（好友 / 全网）、连胜天数、连续打卡徽章。
- 结算分享图：成绩 + 用时 + 连胜天数，一键生成海报。

### 3.8 M7 · 无尽 / 生存
- 连续出题，答对进入下一题，答错/超时即结束。
- 每答对 5 题，难度 +1 且限时缩短 5 秒（下限 30 秒）。
- 记录个人最高连击与历史曲线。

### 3.9 M8 · 教学 / 残局
- **分步引导**：把一道题的解法拆成「先算什么 → 为什么」的两步提示，而非直接给答案。
- **解法树可视化**：用树状图展示 `((3+3)×(8−4))` 的计算层级，帮助理解括号与运算顺序。
- **常见套路库**：`3×8`、`4×6`、`12×2`、`24×1`、`(a−b)×(c+d)` 等经典模型，配例题讲解。
- **错题本**：自动收集做错 / 超时的题，按套路归类，7 天后重练（间隔重复）。

---

## 四、整体界面布局

### 4.1 页面结构（IA）

```
首页 / 模式选择
├── 快速开始（沿用上次模式）
├── 模式卡片网格（M1~M8）
├── 每日挑战入口 + 今日战绩
├── 自定义目标数字入口
└── 底部 Tab：首页 · 闯关 · 对战 · 我的
```

```
游戏页（统一框架，各模式只替换顶部信息条与底部操作区）
├── 顶部信息条：返回 | 模式名/关卡 | 计时环 | 得分 | 设置
├── 中部主区域：牌区 + 表达式构建区 + 数字键盘/运算符区
├── 右侧/底部：提示、撤销、重置、提交、跳过（手机为底部工具栏）
└── 结算浮层：本题解法、用时、得分明细、继续
```

### 4.2 桌面端布局（≥ 1024px，三栏）

```
┌──────────────────────────────────────────────────────────┐
│ Logo   模式选择  每日挑战  排行榜  自定义    [头像]       │ 顶部导航 56px
├────────┬──────────────────────────────────┬──────────────┤
│ 左栏   │            主游戏区               │  右栏        │
│ 220px  │                                  │  280px       │
│        │   ┌────────────────────────┐     │              │
│ 模式   │   │  ⏱ 计时环  Lv.3  ★★☆  │     │  计分板      │
│ 列表   │   └────────────────────────┘     │  · 当前分    │
│        │                                  │  · 连击 x3   │
│ 难度   │   ┌────┐┌────┐┌────┐┌────┐      │  · 用时      │
│ 筛选   │   │ ♠7 ││ ♥3 ││ ♦8 ││ ♣2 │      │              │
│        │   └────┘└────┘└────┘└────┘      │  剩余提示 2  │
│ 设置   │        牌区（可点选）            │              │
│        │                                  │  ─────────   │
│        │   ┌────────────────────────┐     │  历史记录    │
│        │   │ 表达式：7 - 3 = 4  [×] │     │  1. 24 ✓    │
│        │   └────────────────────────┘     │  2. 24 ✓    │
│        │                                  │  3. — ✗     │
│        │   [+] [-] [×] [÷] [ ( ] [ ) ]    │              │
│        │   [ 撤销 ] [ 清空 ] [ 提交 ]      │              │
└────────┴──────────────────────────────────┴──────────────┘
```

### 4.3 移动端布局（< 768px，单列纵向）

```
┌───────────────────────────┐
│ ←  闯关 3-5    ⏱ 38s  ★★ │  顶部 48px
├───────────────────────────┤
│                           │
│   ┌───┐ ┌───┐ ┌───┐       │  牌区：大卡片
│   │ 7 │ │ 3 │ │ 8 │ ...   │  横向排布，可滑动
│   └───┘ └───┘ └───┘       │
│                           │
│  ┌─────────────────────┐  │
│  │ (7-3) × (8-2) = ?   │  │  表达式显示区
│  └─────────────────────┘  │
│                           │
│  7  8  9   ÷              │
│  4  5  6   ×              │  数字 + 运算符键盘
│  1  2  3   −              │  （点选式模式下
│  0  (  )   +              │   数字键位即牌面）
│                           │
│  [撤销] [提示] [提交]      │  底部操作栏 64px
└───────────────────────────┘
```

**移动端适配要点**
- 卡片触控目标 ≥ 56×56px，间距 ≥ 12px。
- 数字键盘固定在底部，避免键盘弹起遮挡。
- 横屏时自动切换为「牌区左 / 键盘右」的双栏。
- 支持**深色模式**跟随系统。

### 4.4 视觉与组件规范
- **主色**：扑克红 `#E5484D`、墨黑 `#1A1A1A`、纸白 `#FFFDF7`；辅助色：竹青（正确）`#2E9E6B`、琥珀（提示）`#F5A524`。
- **卡片**：圆角 16px、轻微阴影、翻转动画（发牌时逐张翻开，间隔 80ms）。
- **字体**：数字使用等宽数字字体（避免计时跳动），中文系统字体。
- **动效**：
  - 提交正确：卡片聚拢 → 光效 + 音效 + 得分飘字；
  - 提交错误：轻微抖动 + 红色描边，0.5s 后恢复；
  - 计时最后 10 秒：计时环变红 + 脉冲 + 心跳音。
- **音效**：可开关；正确 / 错误 / 倒计时 / 连击，均由 Web Audio 合成，无需加载资源。

### 4.5 无障碍
- 全部按钮可键盘操作（Tab / Enter / 数字键选牌、运算符键 `+ - * /`）。
- 卡片带 `aria-label`（如"黑桃 7"），配色之外用**花色符号**区分红黑，不依赖颜色单一线索。
- 支持「减少动效」系统偏好；支持大字号模式。

---

## 五、交互流程

### 5.1 全局状态机

```
[加载] → [首页]
            │ 选择模式
            ▼
        [准备页] ──设置（难度/牌数/限时/扩展运算符）──┐
            │                                        │
            ▼ 开始                                   │
        [发牌] ──> [求解器预计算：可解性 / 难度 / 全部解]
            │
            ▼
        [答题中] ◄──── 撤销 / 清空 / 草稿
            │
       ┌────┴─────────────┬──────────────┐
       ▼                  ▼              ▼
   [提交正确]        [提交错误]      [超时/跳过]
       │                  │              │
   计分+动画          提示错误原因    扣命/扣分
       │                  │              │
       └────────┬─────────┴──────────────┘
                ▼
           [单题结算] ──展示解法、用时、得分明细
                │
        ┌───────┴────────┐
        ▼                ▼
   [下一题]          [本局结束]
        │                │
        └────────────────┴──> [总结算页] → [首页 / 再来一局 / 分享]
```

### 5.2 关键交互细节

**点选式答题（以 `7 3 8 2 → 24` 为例）**
1. 玩家点「7」→ 卡片升起，进入待运算态。
2. 点「−」→ 桌面出现运算符槽位。
3. 点「3」→ 两张牌合并为一张新卡「4」，原卡片淡出，桌面剩 `4 / 8 / 2`。
4. 重复：`8 − 2 = 6` → 桌面剩 `4 / 6`。
5. 点「4」→「×」→「6」→ 合成「24」。
6. 只剩一张牌且值为 24 → **自动提交**并播放胜利动效（也可手动点提交，方便玩家先检查）。

**撤销**：回退一步合成（新卡拆回两张原卡）。支持无限撤销。
**清空**：整局重置，牌面恢复初始。
**提示**（分级消耗）：
| 提示等级 | 内容 | 消耗 |
|---|---|---|
| Lv.1 方向提示 | 提示"先凑 4 和 6"这类**中间目标** | 1 次提示 / 本题得分 ×0.8 |
| Lv.2 首步提示 | 高亮第一步该用哪两张牌与运算符 | 1 次提示 / 本题得分 ×0.6 |
| Lv.3 完整答案 | 直接展示完整表达式 | 1 次提示 / 本题得分为 0（仍计入完成） |

**跳过**：不计分，扣 1 颗心（闯关模式），计入正确率。

---

## 六、难度分级与计分机制

### 6.1 难度模型（自动计算，非人工标注）

求解器对每副牌计算 5 个因子，加权得到**难度分 D（0~100）**：

| 因子 | 说明 | 权重 |
|---|---|---|
| `S` 解的稀缺度 | 本质不同解数量越少越难（1 解最满） | 35% |
| `F` 分数依赖 | 必须使用**除法且中间出现非整数**（如 `3 ÷ (1 − 3/4)`）才难 | 25% |
| `B` 括号深度 | 需嵌套括号 / 非左结合结构 | 15% |
| `C` 反直觉度 | 不在 `3×8 / 4×6 / 12×2 / 24×1` 等常见套路内 | 15% |
| `N` 牌面复杂度 | 牌数越多、含 A/大牌越多 → 越高 | 10% |

难度分 → 等级映射：

| 等级 | 难度分 | 描述 | 建议限时 | 难度系数 K |
|---|---|---|---|---|
| L1 入门 | 0–20 | 一眼可见，常见套路 | 90s | 1.0 |
| L2 简单 | 21–40 | 需一两步尝试 | 75s | 1.3 |
| L3 普通 | 41–60 | 需枚举与排除 | 60s | 1.6 |
| L4 困难 | 61–80 | 解少，需分数中间态 | 50s | 2.0 |
| L5 专家 | 81–100 | 唯一解且极反直觉 | 45s | 2.5 |

> 玩家可在设置里手动锁定难度，或选「自适应」：根据最近 10 题的正确率与用时，**动态滑动**目标难度（正确率高则上浮，连续失败则下调）。

### 6.2 单题计分公式

```
单题得分 = 基础分 × 难度系数 × 时间加成 × 连击倍率 × 提示衰减 − 错误惩罚
```

| 项 | 计算方式 |
|---|---|
| 基础分 | 100 分（4 张牌）；5/6/7 张牌分别为 140 / 190 / 250 |
| 难度系数 K | 见 6.1（1.0 ~ 2.5） |
| 时间加成 | `1 + max(0, (限时 − 用时) / 限时) × 0.5`（上限 1.5 倍） |
| 连击倍率 | 连续答对 n 题：`min(1 + 0.1 × n, 2.0)`；答错/超时归零 |
| 提示衰减 | Lv.1 ×0.8、Lv.2 ×0.6、Lv.3 ×0（见 5.2） |
| 错误惩罚 | 每次错误提交 −10 分（单题扣至 0 为止），**不使用提示不扣** |
| 多解奖励 | 同一题提交第 2 种及以上的正确解法，每种 +20 分 |

**举例**：L4 难度 6 张牌题，60 秒内 20 秒答对，当前连击 5，无提示无错误：
`190 × 2.0 × (1 + 40/60×0.5) × 1.5 = 190 × 2.0 × 1.33 × 1.5 ≈ 758 分`

### 6.3 闯关 / 无尽模式的额外机制
- **星级**：见 3.4，星星解锁章节与奖励。
- **关卡奖励**：首次通关 +200，三星通关额外 +300。
- **复活**：消耗 1 枚复活币（每日登录赠送 3 枚，或分享获得）。
- **无尽模式**：分数按 `连击倍率` 累积，难度每 5 题 +1，限时每 5 题 −5s（下限 30s）。

### 6.4 多人竞技计分（抢答制）

| 事件 | 得分 |
|---|---|
| 第一个答对 | `100 + 剩余时间 × 2`（如剩 20s → 140 分） |
| 第二个答对 | 60 分 |
| 第三个及以后答对 | 30 分 |
| 答错 | −15 分，并冻结 3 秒 |
| 超时未答 | 0 分 |
| 一题全对奖励 | 全员答对时，每人额外 +20 |

**天梯分（可选）**：房间赛累计积分，跨房间按 ELO 微调（K=32），段位：青铜 → 白银 → 黄金 → 钻石 → 大师，每赛季重置并发放徽章。

### 6.5 成就与激励
- 成就体系：`首次通关`、`连续 10 连击`、`L5 专家题 5 秒解`、`累计 1000 题`、`一周全勤`、`击败好友 10 次`。
- 进度可视化：个人主页展示**难度雷达图**（速度 / 准确率 / 难度承受 / 稳定性 / 多解发现）、30 天热力图。

---

## 七、数据与存储设计（概念层）

| 数据 | 存储位置 | 说明 |
|---|---|---|
| 玩家档案 / 设置 / 本地进度 | 浏览器 IndexedDB | 免登录可玩 |
| 闯关进度、错题本、成就 | IndexedDB（可选云同步） | 登录后可跨设备 |
| 每日挑战题目 | 服务端按日期种子生成 | 保证全网一致 |
| 排行榜 / 天梯分 | 服务端 | 需登录 |
| 房间对战状态 | 服务端内存 + WebSocket | 房间关闭即释放 |
| 题库缓存 | IndexedDB | 预生成 L1~L5 牌组各 500 组，离线可用 |

**账号**：支持「游客 → 微信/手机号一键绑定」，避免强制注册流失。

---

## 八、技术架构建议（仅作选型说明）

- **前端框架**：React 或 Vue 3 + TypeScript；状态管理用轻量方案（Zustand / Pinia）。
- **核心算法**：独立的 `solver` 模块（纯函数、有理数运算），单测覆盖；运行在 **Web Worker** 中，5 张以上牌时避免 UI 卡顿。
- **样式**：Tailwind CSS 或 CSS Modules + CSS 变量做主题切换（浅色/深色）。
- **动画**：Framer Motion（或 CSS transition + Web Animations API）实现发牌、合并、结算动效。
- **实时对战**：WebSocket（或 Server-Sent Events + HTTP 提交，低成本方案）。
- **部署**：静态站点托管 + CDN；注册为 PWA，支持添加到主屏。
- **性能预算**：首屏 < 1.5s（3G 网络），交互响应 < 100ms，包体积 < 300KB（gzip）。

---

## 九、开发路线图（建议）

| 阶段 | 内容 | 目标 |
|---|---|---|
| 第 1 周 | 求解器 + 发牌器 + 难度评估（含单测） | 内核稳定，无解判定 100% 准确 |
| 第 2 周 | M1 经典模式 + 点选式交互 + 键盘输入 | 可玩 Demo |
| 第 3 周 | M3 闯关 + 星级 + 计分系统 + 本地存档 | 单人闭环 |
| 第 4 周 | M2 多张牌 + M5 自定义目标 + 提示系统 | 玩法矩阵成型 |
| 第 5–6 周 | M4 多人房间对战 + M6 每日挑战 + 排行榜 | 社交与留存 |
| 第 7 周 | 教学/残局、成就、数据看板、PWA | 长期留存 |
| 第 8 周 | 打磨动效音效、无障碍、性能优化、灰度发布 | 上线 |

---

## 十、关键风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 玩家卡在某题导致流失 | 高 | 分级提示 + 60 秒后自动降难度 + 允许跳过 |
| 多张牌求解性能 | 中 | Web Worker + 预生成题库 + 求解超时兜底 |
| 竞技模式作弊（脚本秒解） | 中 | 提交频率限制、异常检测、天梯与休闲分榜 |
| 低龄用户看不懂提示 | 中 | M8 教学模式 + 分步引导 + 家长报告 |
| 无解牌组引发挫败 | 中 | 默认保证有解，仅在"判断无解"玩法中开放 |

---

*文档结束 · 下一步可细化：交互原型（线框图 → 高保真）或核心算法伪代码设计。*
